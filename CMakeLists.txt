cmake_minimum_required(VERSION 3.13)
project(mpi-cuda)
enable_language(CUDA)
find_package(MPI)
add_compile_options()
file(GLOB SRC "src/*.c")
add_executable(${PROJECT_NAME} ${SRC})
install(TARGETS ${PROJECT_NAME} DESTINATION /usr/bin)
target_link_libraries(${PROJECT_NAME} MPI::MPI_C m)
