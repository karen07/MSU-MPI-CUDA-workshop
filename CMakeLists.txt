cmake_minimum_required(VERSION 3.10)
project(mpi_cuda)
enable_language(CUDA)
find_package(MPI)
add_compile_options()
add_executable(${PROJECT_NAME} src/mpi_cuda.cu)
install(TARGETS ${PROJECT_NAME} DESTINATION /usr/bin)
target_link_libraries(${PROJECT_NAME} MPI::MPI_C m)
